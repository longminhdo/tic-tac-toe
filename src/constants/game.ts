export enum Player {
  PLAYER_1 = 'player_1',
  PLAYER_2 = 'player_2',
  COMPUTER = 'computer',
}

export enum GameSize {
  BASIC = 3,
}

export enum Mark {
  CROSS = 'cross',
  NOUGHT = 'nought',
}

export enum Turn {
  FIRST = 0,
  SECOND = 1,
}

export enum WinCondition {
  BASIC = 3,
}

export enum Result {
  WIN = 'win',
  LOSE = 'lose',
  DRAW = 'draw',
}
