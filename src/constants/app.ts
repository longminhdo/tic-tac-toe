export enum Theme {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum IconButtonSize {
  MOBILE = 26,
  DESKTOP = 36,
}

export enum Breakpoints {
  XS = 480,
  SM = 576,
  MD = 768,
  LG = 992,
  XL = 1200,
  XXL = 1600,
}
